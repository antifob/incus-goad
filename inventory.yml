---
#
# Site-specific Ansible variables
#
# If you'd like to override shared, group- or host-specific variables,
# it can be done here.
#
all:
  # all hosts
  vars:

  # host-specific
  hosts:
    lx01: # DRACARYS
      # FIXME ideally this would be put elsewhere tofu can also pick up
      ansible_incus_project: default
      ansible_incus_remote: local

  # group-specific
  children:
    # see groups `GOAD/ad/$lab/data/inventory`
    linux_domain:
      vars:
        ansible_incus_project: default
        ansible_incus_remote: local
